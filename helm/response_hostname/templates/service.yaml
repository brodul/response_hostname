apiVersion: v1
kind: Service
metadata:
  name: {{ include "response_hostname.fullname" . }}
  labels:
    {{- include "response_hostname.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "response_hostname.selectorLabels" . | nindent 4 }}
