apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "response_hostname.fullname" . }}-test-connection"
  labels:
    {{- include "response_hostname.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "response_hostname.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
